@model JiraApp.Models.Tasks.TaskItem

<div class="modal-header">
    <h5 class="modal-title">Task Details: @Model.Title</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<div class="modal-body">
    <partial name="_TaskDetailsPartial" model="Model" />
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
    <a href="@Url.Action("Edit", "Task", new { id = Model.Id })" class="btn btn-primary">Edit Task</a>
</div>
